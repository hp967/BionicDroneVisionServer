name: Build iOS IPA

on:
  workflow_dispatch:

jobs:
  build:
    name: Build iOS App
    runs-on: macos-14
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    
    - name: Setup Xcode
      uses: maxim-lobanov/setup-xcode@v1
      with:
        xcode-version: '15.4'
    
    - name: Create iOS App
      run: |
        mkdir -p Sources
        
        cat > Sources/main.swift << 'EOF'
        import SwiftUI
        @main
        struct App: App {
            var body: some Scene {
                WindowGroup {
                    Text("Hello")
                }
            }
        }
        EOF
        
        cat > Sources/Info.plist << 'EOF'
        <?xml version="1.0" encoding="UTF-8"?>
        <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
        <plist version="1.0">
        <dict>
            <key>CFBundleExecutable</key><string>$(EXECUTABLE_NAME)</string>
            <key>CFBundleIdentifier</key><string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
            <key>CFBundleInfoDictionaryVersion</key><string>6.0</string>
            <key>CFBundleName</key><string>$(PRODUCT_NAME)</string>
            <key>CFBundlePackageType</key><string>APPL</string>
            <key>CFBundleShortVersionString</key><string>1.0</string>
            <key>CFBundleVersion</key><string>1</string>
            <key>LSRequiresIPhoneOS</key><true/>
            <key>UILaunchScreen</key><dict/>
            <key>UIRequiredDeviceCapabilities</key><array><string>arm64</string></array>
            <key>UISupportedInterfaceOrientations</key><array><string>UIInterfaceOrientationPortrait</string></array>
        </dict>
        </plist>
        EOF
        
        ls -la Sources/
    
    - name: Generate Xcode Project
      run: |
        brew install xcodegen
        
        cat > Project.yml << 'EOF'
        name: TestApp
        options:
          bundleIdPrefix: com.test
          deploymentTarget:
            iOS: "16.0"
        targets:
          TestApp:
            type: application
            platform: iOS
            sources:
              - path: Sources
            settings:
              base:
                CODE_SIGNING_REQUIRED: NO
                CODE_SIGNING_ALLOWED: NO
                PRODUCT_BUNDLE_IDENTIFIER: com.test.app
                IPHONEOS_DEPLOYMENT_TARGET: 16.0
        EOF
        
        xcodegen generate
        ls -la *.xcodeproj/
    
    - name: Build and Archive
      run: |
        mkdir -p build
        
        echo "=== Starting xcodebuild archive ==="
        xcodebuild archive \
          -project TestApp.xcodeproj \
          -scheme TestApp \
          -configuration Release \
          -destination 'generic/platform=iOS' \
          -archivePath build/TestApp.xcarchive \
          CODE_SIGNING_ALLOWED=NO \
          2>&1 | tee build.log
        
        echo "=== Checking result ==="
        if [ -d "build/TestApp.xcarchive" ]; then
            echo "SUCCESS: Archive created"
            ls -la build/TestApp.xcarchive/
            ls -la build/TestApp.xcarchive/Products/Applications/ 2>/dev/null || echo "No Applications dir"
        else
            echo "FAILED: Archive not found"
            echo "=== Last 100 lines of build.log ==="
            tail -100 build.log
            exit 1
        fi
    
    - name: Export IPA
      if: success()
      run: |
        cat > export.plist << 'EOF'
        <?xml version="1.0" encoding="UTF-8"?>
        <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
        <plist version="1.0">
        <dict>
            <key>method</key><string>development</string>
            <key>uploadSymbols</key><false/>
        </dict>
        </plist>
        EOF
        
        xcodebuild -exportArchive \
          -archivePath build/TestApp.xcarchive \
          -exportPath build \
          -exportOptionsPlist export.plist \
          2>&1 | tee export.log
        
        ls -la build/*.ipa 2>/dev/null || echo "IPA not created"
    
    - name: Upload Logs
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: build-logs
        path: |
          build.log
          export.log
        if-no-files-found: warn
    
    - name: Upload IPA
      if: success()
      uses: actions/upload-artifact@v4
      with:
        name: TestApp
        path: build/*.ipa
        if-no-files-found: error
