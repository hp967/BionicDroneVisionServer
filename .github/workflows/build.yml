name: Test Build

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  test:
    name: Test
    runs-on: macos-latest
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    
    - name: Print Hello
      run: echo "Hello from GitHub Actions!"
    
    - name: Check Xcode
      run: |
        xcodebuild -version
        swift --version
